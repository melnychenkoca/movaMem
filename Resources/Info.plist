<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>movaMem</string>
    <key>CFBundleDisplayName</key>
    <string>movaMem</string>
    <key>CFBundleIdentifier</key>
    <string>com.movamem.app</string>
    <key>CFBundleExecutable</key>
    <string>movaMem</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <!-- Names AppIcon.icns in Contents/Resources, without the extension. The
         file is generated by `make icon`, not checked in. -->
    <key>CFBundleIconFile</key>
    <string>AppIcon</string>
    <!-- The release is driven from this value: the workflow refuses to publish
         unless the pushed tag is v<CFBundleShortVersionString>, which keeps the
         tag, the app, and the Homebrew cask from drifting apart. -->
    <key>CFBundleShortVersionString</key>
    <string>1.1.0</string>
    <!-- The build number. macOS treats this, not the version string above, as
         what distinguishes one build from another, so it must increase on every
         release. Keeping it equal to the version string is the simplest rule
         that satisfies that. -->
    <key>CFBundleVersion</key>
    <string>1.1.0</string>
    <key>LSMinimumSystemVersion</key>
    <string>14.0</string>
    <!-- LSUIElement is what makes this a menu-bar-only app: no Dock icon, no
         window, no app switcher entry. Without it the app appears in the Dock. -->
    <key>LSUIElement</key>
    <true/>
</dict>
</plist>
