# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Build output — the assembled app bundle, the release zip, and the generated
# icon (drawn by Tools/make-icon.swift, never edited by hand)
movaMem.app/
movaMem.zip
Resources/AppIcon.icns

# Subagent-driven development scratch (ledger, briefs, review packages)
.superpowers/

# macOS
.DS_Store

# Xcode (not used, but harmless to guard against)
*.xcuserstate
xcuserdata/
